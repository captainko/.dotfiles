#!/bin/sh
# quickly navigate to projects
. "$HOME/.scripts/_z"

# ls="ls --color=yes -lah --group-directories-first" \
alias aptu="sudo apt update" \
	aptug="sudo apt upgrade" \
	less="less -XR" \
	cl="clear" \
	pd="cd ~-" \
	gh='history|grep' \
	lst="!:0" \
	ls="exa -lah" --group-directories-first \
	ll="ls --color=always -lah | less -R" \
	lsa="ls -la" \
	df="df -h" \
	mx='cmatrix -C magenta' \
	sclip='xclip -sel clip' \
	gclip='xclip -selection c -o' \
	fh='nohup nautilus . &>/dev/null & ; clear;' \
	t='tmux' \
	tk='tmux kill-server' \
	tka='tmux kill-session -a' \
	yt='youtube-dl --add-metadata -i' \
	yta='yt -x --audio-format mp3 --audio-quality 0' \
	d='docker' \
	psdocker="sudo systemctl stop docker.service" \
	stdocker="sudo systemctl start docker.service" \
	k='kubectl' \
	c='code' \
	cfsc='vim ~/.scripts/_z' \
	cfa='vim ~/.config/aliasrc' \
	lg="lazygit" \
	cfg='git config --global -e' \
	cfat='vim ~/.config/alacritty/alacritty.yml' \
	cfd='vim ~/.scripts/_z' \
	cfe='vim ~/.config/env' \
	cfeh="sudo vim /etc/hosts" \
	cfi='vim ~/.config/regolith/i3/config' \
	cflg='git config --local -e' \
	cfpa="vim ~/.config/pgadmin/pgadmin4.conf" \
	cfpg="vim ~/.config/pg/psqlrc" \
	cft='vim ~/.config/tmux/tmux.conf' \
	cfto='vim ~/.config/terminator/config' \
	cfk="vim ~/.config/kitty/kitty.conf" \
	cdv="cd ~/.config/nvim/" \
	cfv='cdv; vim init.lua; pd;' \
	cfvs='vim ~/.config/nvim/lua/settings.lua' \
	cfvk='vim ~/.config/nvim/keys/mapping.vim' \
	cfvp='vim ~/.config/nvim/vim-plug/plugs.vim' \
	cfz='vim ~/.config/zsh/.zshrc' \
	dmenu_run='dmenu_run -sb "#CE53C7" -fn "10x20"' \
	cp='cp -iv' \
	cfssh="vim ~/.ssh/config" \
	mpv='swallow mpv' \
	cD='cd ~/Downloads' \
	tt='trash' \
	v='vim' \
	g='git' \
	ga='git add' \
	gc='git checkout' \
	gcb='git checkout -b' \
	gcm='git checkout main' \
	gfh='git fetch' \
	gb='git branch' \
	gbl='git branch -a' \
	glg='git log --stat' \
	gmm='git merge origin/master || git merge origin/main' \
	gph='git push' \
	gpl='git pull' \
	gsh='git stash' \
	gwl='git worktree list' \
	gw='git worktree' \
	gsp='git stash pop' \
	gst='git status' \
	ns='npm start' \
	nsd='npm run start:dev' \
	ndd='npm run start:debug' \
	ngs='ng serve' \
	uget='wget --user-agent="Mozilla/5.0 (X11; Fedora; Linux x86_64; rv:52.0) Gecko/20100101 Firefox/52.0"' \
	fl='flutter' \
	pixie='emulator @pixie' \
	systart='sudo systemctl start' \
	systop='sudo systemctl stop' \
	systus='systemctl status --no-pager' \
	syen='sudo systemctl enable' \
	sydis='sudo systemctl disable' \
	stredis='systart redis-server.service; systus redis-server.service --no-pager' \
	paredis='systop redis-server.service; systus redis-server.service --no-pager' \
	stapche='systart apache2.service; systus mysql.service --no-pager' \
	paapche='systop apache2.service; systus mysql.service --no-pager' \
	stmysql='systart mysql.service; systus mysql.service --no-pager' \
	pamysql='systop mysql.service; systus mysql.service --no-pager' \
	stpgsql='systart postgresql.service; systus postgresql.service --no-pager' \
	papgsql='systop postgresql.service; systus postgresql.service --no-pager' \
	show_services='service --status-all' \
	vim='nvim' \
	ovim='\vim'

# alias dot='dotnet'
# alias dotbl='dotnet build -c Release --runtime linux-x64'
# alias dotbw='dotnet build -c Release --runtime win10-x64'
# alias dotw='dotnet watch run'
#alias ssh='ssh --color'
#alias apt='sudo apt'
# alias zathura='swallow zathura'
#alias android-studio='android-studio'
# alias gas='git submodule foreach --recursive git add -A .'
# alias gcas="git submodule foreach --recursive 'git commit -m \"Committing in a submodule\"'"
# alias nsgc='nest g co';
# alias nsgs='nest g s';
# alias nsgm='nest g mo';
